{
  "name": "tiktok-mod",
  "version": "1.0.0",
  "description": "TikTok DM emoji automation with Telegram control (Ubuntu/Debian)",
  "main": "src/index.js",
  "scripts": {
    "start": "sh -c 'pm2 describe tiktok-mod >/dev/null 2>&1 && pm2 restart ecosystem.config.js || pm2 start ecosystem.config.js'",
    "stop": "pm2 stop tiktok-mod",
    "restart": "pm2 restart tiktok-mod",
    "logs": "pm2 logs tiktok-mod",
    "setup": "bash install.sh"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  },
  "os": [
    "linux"
  ],
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "node-cron": "^3.0.3",
    "playwright": "^1.50.1",
    "telegraf": "^4.16.3",
    "winston": "^3.17.0"
  }
}
